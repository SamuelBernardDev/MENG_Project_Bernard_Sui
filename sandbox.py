# Trial code for testing the functionality of the codebase
import pandas as pd
import numpy as np

liste = [1, 2, 3, 4, 5]
df = pd.DataFrame(liste, columns=["a"])
print(df)
print(df["a"].values)
print("hello world")
